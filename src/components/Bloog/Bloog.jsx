import PropTypes from 'prop-types';

export default function Bloog({blog}){

    console.log(blog)
    return(
        <div>

        </div>
    )
}

Bloog.PropTypes={
    Bloog:PropTypes.object.isRequired

}